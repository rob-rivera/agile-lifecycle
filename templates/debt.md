# <Project> — Debt Registry

> Observed-but-unfixed structural debt. **Debt is to structure what `observed` is to intent:
> recorded, awaiting a human promotion.** Entries are findings, not stories — they are fed by
> implementer reports at story/fix close (the affirmative gate: entries or "debt: none"),
> consumed by `refactor-pass` at intake (target-area entries become pre-loaded findings), and
> promoted to stories through `write-stories` only when a human decides the paydown has earned
> business value.

| Id | Area | The tell (`file:line`) | Found during | Date | Status |
| --- | --- | --- | --- | --- | --- |

<!-- Status: open → routed (became a REF pass finding or a story) → retired (fixed, cite the
     REF/STORY/BUG that retired it). Declined-forever is also `retired`, with a one-word reason. -->
